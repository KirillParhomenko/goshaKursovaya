import InnerWrapper from "./InnerWrapper";

import "./SearchBarStyle.css";

const Form = () => {
  return (
    <form className='searchForm'>
      <InnerWrapper />
    </form>
  );
};

export default Form;
